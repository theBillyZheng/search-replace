search-replace-client contains the front-end react code
- run npm install first
- start the app using npm start

search-replace-service contains the Node.js API endpoints
- run npm install first
- start the app using npm start